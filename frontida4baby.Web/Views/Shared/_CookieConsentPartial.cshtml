<div id="cookie-toast" class="st-cookie-toast" style="display:none;" role="alert" aria-live="polite">
    <div class="cookie-toast-icon">
        <span class="material-symbols-outlined" style="font-size:1.5rem">cookie</span>
    </div>
    <div class="cookie-toast-body">
        <strong>@L["CookieTitle"]</strong>
        <p class="mb-0 mt-1">@L["CookieMessage"] <a asp-controller="Home" asp-action="Privacy" class="cookie-link">@L["CookieLearnMore"]</a></p>
    </div>
    <div class="cookie-toast-actions">
        <button id="cookie-necessary" class="cookie-necessary-btn" aria-label="Μόνο απαραίτητα">
            Μόνο απαραίτητα
        </button>
        <button id="cookie-accept" class="cookie-accept-btn" aria-label="@L["CookieAccept"]">
            @L["CookieAccept"]
        </button>
    </div>
</div>

<script>
(function () {
    if (localStorage.getItem('cookieConsent')) return;
    var toast = document.getElementById('cookie-toast');
    setTimeout(function () { toast.style.display = 'flex'; }, 800);
    function dismiss(value) {
        toast.style.opacity = '0';
        toast.style.transition = 'opacity .25s';
        setTimeout(function () { toast.style.display = 'none'; }, 260);
        localStorage.setItem('cookieConsent', value);
    }
    document.getElementById('cookie-accept').addEventListener('click', function () { dismiss('accepted'); });
    document.getElementById('cookie-necessary').addEventListener('click', function () { dismiss('necessary-only'); });
})();
</script>
